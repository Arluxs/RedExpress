import React from 'react';

// Define el componente como una función que devuelve JSX
const MiComponente = () => {
  return (
    <div>
      <h1>Hola, este es mi primer componente en React!</h1>
      <p>Bienvenido al maravilloso mundo de React.</p>
    </div>
  );
}

// Exporta el componente para que pueda ser utilizado en otros archivos
export default MiComponente;
